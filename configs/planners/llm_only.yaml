# llm_only baseline — the LLM scores fixed candidate strings. Affordance OFF, monitor OFF
# (open-loop), no shield/router. Kept faithful so it stays the failure baseline (docs/04, 07).
name: llm_only
propose: llm_only
alpha: 1.0
beta: 0.0
affordance: false
clarification: false
safety: false
monitor: false        # open-loop: no closed-loop goal check
max_retries: 0        # open-loop: no retry/replan on failure (keeps it the faithful weak baseline)
max_steps: 8
llm:
  model: claude-haiku-4-5
  temperature: 0.0
